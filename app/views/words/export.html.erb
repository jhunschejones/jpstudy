<div class="words-export-page">
  <h1 class="page-title">Export your word list</h1>

  <p>
    Download a CSV of your word list for data backup or to use in another study tool. Choose to export all words you have created, or just the words marked as "cards not created."
  </p>

  <div class="actions">
    <%= link_to(download_words_path(format: :csv), target: "_blank", class: "button-blue export-button") do %>
      <%= heroicon "document-download", variant: :outline, options: { class: "download-icon" } %>
      All words
    <% end %>
    <%= link_to(download_words_path(format: :csv, filter: "cards_not_created"), target: "_blank", class: "button-blue export-button") do %>
      <%= heroicon "document-download", variant: :outline, options: { class: "download-icon" } %>
      Words without cards
    <% end %>
  </div>

  <br/>

  <% if @japanese_words_without_cards.size > 0 %>
    <h2 class="section-title">Words without cards</h2>

    <p>Just need to grab the <%= pluralize @japanese_words_without_cards.size, "new word" %> you added? Here's a quick place to do it all at once!</p>
    <div class="new-words" lang="jp">
      <%= @japanese_words_without_cards.join(", ") %>
    </div>
  <% end %>
</div>
